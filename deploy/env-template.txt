# MesaHomes Environment Variables
# Copy to .env and fill in values before deploying.
# NEVER commit .env to git.

# AWS
AWS_REGION=us-west-2
MESAHOMES_TABLE=mesahomes-main
S3_DATA_BUCKET=mesahomes-data
S3_PHOTOS_BUCKET=mesahomes-property-photos

# Cognito
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=

# Google
# mesahomes/google-maps-api-key in Secrets Manager
# 10,000 free Street View images/month

# Stripe (Virtual Home Zone — FSBO tier)
VIRTUAL_HOME_ZONE_STRIPE_ACCOUNT_ID=
VIRTUAL_HOME_ZONE_STRIPE_PUBLIC_KEY=
VHZ_STRIPE_WEBHOOK_SECRET=

# VHZ Stripe handoff (Blocker 1 — Approach A)
VHZ_HANDOFF_SECRET=
VHZ_MESAHOMES_WEBHOOK_SECRET=
VHZ_CHECKOUT_BASE_URL=https://virtualhomezone.com/checkout

# Stripe (MesaHomes — flat-fee tier, gated)
# mesahomes/stripe-secret-key in Secrets Manager
# mesahomes/stripe-webhook-secret in Secrets Manager

# Brokerage (set when broker partnership is active)
LISTINGS_PAYMENT_ENABLED=false
BROKER_OF_RECORD_NAME=
BROKER_OF_RECORD_LICENSE=
BROKER_OF_RECORD_BROKERAGE=

# Frontend (NEXT_PUBLIC_ prefix for static export)
NEXT_PUBLIC_API_BASE=/api/v1
NEXT_PUBLIC_LISTINGS_PAYMENT_ENABLED=false
NEXT_PUBLIC_BROKER_OF_RECORD_NAME=
NEXT_PUBLIC_BROKER_OF_RECORD_LICENSE=
NEXT_PUBLIC_BROKER_OF_RECORD_BROKERAGE=

# SES
# mesahomes/ses-smtp-credentials in Secrets Manager

# RentCast (backup, 50 free calls/month)
# mesahomes/rentcast-api-key in Secrets Manager
